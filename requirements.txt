# DEPRECATED: Use pyproject.toml instead.
#   pip install .          (core only)
#   pip install .[all]     (all optional dependencies)
#   pip install -e .[all]  (development, editable install)

setuptools
customtkinter>=5.2.0
pywinauto>=0.6.8;sys_platform=="win32"
